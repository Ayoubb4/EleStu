services:
  - type: web
    name: elestu-be
    env: node
    plan: free
    region: oregon
    buildPacks:
      - url: https://github.com/jontewks/puppeteer-heroku-buildpack.git
    buildCommand: "npm install && npm run build && npm run typeorm -- migration:run"
    startCommand: "npm run start:prod"